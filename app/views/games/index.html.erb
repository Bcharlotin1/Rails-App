
<br><br><br><br><br>

add buttons  edit for users who created a game or any body cna edit ?
add button to  add review

<button><h3><%= link_to "New Game", new_game_path(@game) %></h3></button>

<%@games.each do |game|%>

    <div class="row">
        <div class="column">
            <img src="<%= game.image%>" >
            <h3><%= link_to game.title, game_path(game) %></h3>
            <p> <%=game.description%> </p>
            <button><h4> <%= link_to game.category.name, category_path(game.category)%> </h4></button>
        </div>
    </div>

<%end%>